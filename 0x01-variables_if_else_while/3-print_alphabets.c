#include <stdio.h>
/**
 * main - the beginning
 * Return: is 0
 */
int main(void)
{
	char c, d;

	c = 'a';
	d = 'A';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	while
		(d <= 'Z') {
			putchar(d);
			d++;
		}
	putchar('\n');
	return (0);
}
